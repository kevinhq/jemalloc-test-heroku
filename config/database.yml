# SQLite version 3.x
#   gem install sqlite3
development:
  adapter: postgresql
  database: newthl_development
  pool: 5
  username: postgres

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: newthl_test
  pool: 5
  username: postgres

